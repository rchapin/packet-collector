#
# PacketCollector configuration file.
#

configVersion = "0.1";

logging = {
  level = "trace";
  file  = "/var/log/nta/packet-collector.log"
};

memory = {
  maxSize  = 4096LL;
  warnSize = 2048LL;
};

ObjectManager = {

};

PacketWriters : (
  {
  id    = "a";
  class = "StandardOut";
  shutdown : { timeout = 5000; };
  }  
);

Pcaps : (
  {
  id                  = "foo";
  writers             = ( "a" );
  startOnWriterReady  = true;
  }
);
